{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'node /app/scripts/fly/bootstrap-config.mjs && exec node dist/index.js gateway --allow-unconfigured --port ${PORT:-3000} --bind lan'",
    "healthcheckPath": "/v1/models",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
